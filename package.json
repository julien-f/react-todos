{
  "private": true,
  "devDependencies": {
    "husky": "^0.14.3",
    "lint-staged": "^4.2.3",
    "prettier": "^1.7.4"
  },
  "lint-staged": {
    "*.js": [
      "prettier --write",
      "git-add"
    ]
  },
  "scripts": {
    "build": "cd pages && yarn run build",
    "precommit": "lint-staged",
    "start": "cd server && yarn run start"
  },
  "workspaces": [
    "pages",
    "server"
  ]
}
